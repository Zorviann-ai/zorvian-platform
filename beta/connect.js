const form=document.getElementById('f');const statusBox=document.getElementById('status');form.onsubmit=async e=>{e.preventDefault();statusBox.textContent='Connecting…';const body={email:document.getElementById('email').value,password:document.getElementById('password').value};const otp=document.getElementById('otp').value.trim();if(otp)body.otp=otp;try{const r=await fetch('/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.detail||'Login failed');const token=d.token||d.access_token||d.session_token;if(!token)throw new Error('Core returned no session token');sessionStorage.setItem('zorvian_beta_token',token);document.getElementById('password').value='';statusBox.textContent='Connected to Zorvian Core. You can now open the Beta Hub.'}catch(err){sessionStorage.removeItem('zorvian_beta_token');statusBox.textContent='Connection failed: '+err.message}};